<UserControl
    x:Class="NightGlow.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:nightGlow="clr-namespace:NightGlow"
    xmlns:local="clr-namespace:NightGlow.Views"
    mc:Ignorable="d" 
    Width="400"
    Height="400"
    >
    <StackPanel
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        >

        <TextBlock>
            <Run Text="Brightness" />
            <Run Text="{Binding NightGlowService.Brightness}" />
        </TextBlock>

        <TextBlock>
            <Run Text="Temperature" />
            <Run Text="{Binding NightGlowService.Temperature}" />
        </TextBlock>

        <Button
            Click="ButtonVersion_OnClick"
            Style="{StaticResource MaterialDesignFlatButton}"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            >
            <TextBlock
                FontSize="12"
                Background="{x:Null}"
                >
                <Run Text="v" />
                <Run Text="{x:Static nightGlow:App.VersionString}" />
                <Run Text=" ↗" />
            </TextBlock>
        </Button>

    </StackPanel>

</UserControl>
